# Web & API Server
fastapi==0.103.1
uvicorn==0.23.2
websockets==11.0.3
pyzmq==25.1.1

# Database Layer — single source of truth is db/requirements.txt
# (asyncpg, h3, and friends), included here so `pip install -r
# requirements.txt` at repo root still gets a working, non-conflicting
# set of versions instead of two files silently disagreeing.
-r db/requirements.txt
pandas==2.1.0

# GNN Engine (Moved from ml/gnn_engine/requirements.txt)
torch>=2.0.0
torch-geometric>=2.4.0
scikit-learn==1.3.0

# Utilities
python-dotenv==1.0.0